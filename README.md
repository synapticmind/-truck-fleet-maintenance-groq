# 🚛 Truck-Fleet-Maintenance-Groq
> **Zero-cost MVP** – Excel upload → Supabase → Groq → Streamlit risk dashboard.

## 🚀 Quick Start (local)
```bash
pip install -r requirements.txt
streamlit run app.py
